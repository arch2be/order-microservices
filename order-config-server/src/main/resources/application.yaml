server:
  port: 8888
spring:
  security:
    basic:
      enabled: true
    user:
      name: user
      password: user
  cloud:
    config:
      server:
        git:
          uri: https://github.com/arch2be/sample-config-application
          force-pull: true
          skipSslValidation: true
          timeout: 10

